import { GridColDef } from "@mui/x-data-grid";

const useColumnsPurchaseReception = () => {
  
  const columns = (): GridColDef[] => {
    return []
  }

  return { columns };
};

export default useColumnsPurchaseReception;
